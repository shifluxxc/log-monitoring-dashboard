# Server Configuration
PORT=7000
NODE_ENV=production

# Database Configuration (Docker Network)
# When running in Docker containers, use the service name
DB_HOST=postgres
DB_PORT=5432
DB_NAME=log_management
DB_USER=postgres
DB_PASSWORD=password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# Optional: Enable detailed logging
DEBUG=false 